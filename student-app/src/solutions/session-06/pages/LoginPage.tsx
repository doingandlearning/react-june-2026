export function LoginPage() {
  return (
    <div>
      <h1>Sign in</h1>
      <p>Authentication is out of scope for this lab.</p>
      <p>Flip <code>IS_AUTHENTICATED</code> in <code>RequireAuth.tsx</code> to test protected routes.</p>
    </div>
  );
}
